machine:
    php:
        version: 7.1.0
    environment:
        FRAMEWORK_ENV: prod
        FRAMEWORK_DEBUG: false
        DATABASE_NAME: simplex
        DATABASE_USER: ubuntu
        DATABASE_PASS:

database:
    override:
        - composer init-db

test:
    pre:
        - composer validate
    override:
        - vendor/bin/phpunit
